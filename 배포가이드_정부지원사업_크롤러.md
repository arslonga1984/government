# 정부 지원사업 자동 매칭 크롤러 — 배포 가이드

## 개요

이 문서는 **정부 지원사업 자동 매칭 크롤러**를 실제 운영 환경에 배포하는 전체 절차를 안내합니다.

배포 방식은 두 가지입니다:
- **방법 A: Docker 배포** (권장) — Docker만 설치하면 한 번에 완료
- **방법 B: 서버 직접 설치** — Docker 없이 Python 직접 설치

---

## 사전 준비 (공통)

### 1. 기업마당 API 키 발급

기업마당(Bizinfo)은 공식 REST API를 제공하며, 이것이 가장 포괄적인 데이터소스입니다.

1. https://www.bizinfo.go.kr 접속 → 회원가입 / 로그인
2. 상단 메뉴에서 **활용정보 → 정책정보 개방** 클릭
   - 또는 직접 접속: https://www.bizinfo.go.kr/web/lay1/program/S1T175C174/apiList.do
3. **API 인증키 발급 및 사용자 등록** → **신청하기** 클릭
4. 신청 사유 작성 (예: "중소기업 지원사업 정보 수집 및 사내 공유 목적")
5. 승인 후 발급된 **crtfcKey** (인증키) 값을 복사하여 보관

> 승인까지 1~3 영업일 소요될 수 있습니다. 승인 전에도 HTML 폴백 크롤링으로 동작합니다.


### 2. 이메일 SMTP 설정

발송용 이메일 계정의 SMTP 정보가 필요합니다.

**Gmail 사용 시 (권장):**

1. https://myaccount.google.com 접속
2. **보안** → **2단계 인증** 활성화 (필수)
3. **보안** → **앱 비밀번호** → 앱 선택: "메일", 기기 선택: "기타(맞춤 이름)"
4. 생성된 **16자리 앱 비밀번호** 복사 (이것이 SENDER_PASSWORD)

| 항목 | Gmail 설정값 |
|------|-------------|
| SMTP_SERVER | `smtp.gmail.com` |
| SMTP_PORT | `587` |
| SENDER_EMAIL | `your-email@gmail.com` |
| SENDER_PASSWORD | `xxxx xxxx xxxx xxxx` (앱 비밀번호) |

**네이버 메일 사용 시:**

1. 네이버 메일 → 환경설정 → POP3/IMAP 설정 → **IMAP/SMTP 사용** 체크
2. SMTP 서버: `smtp.naver.com`, 포트: `587`

**회사 메일 서버 사용 시:**

IT팀에 SMTP 서버 주소, 포트, 인증 정보를 확인하세요.


### 3. (선택) Claude API 키

키워드 매칭만으로도 충분히 동작하지만, LLM 기반 정밀 매칭을 원하면:

1. https://console.anthropic.com 에서 API 키 발급
2. `LLM_MATCHING=true`로 설정

---

## 방법 A: Docker 배포 (권장)

### Step 1: 서버 준비

최소 사양: **Ubuntu 22.04+**, 1 vCPU, 1GB RAM, 10GB 디스크

```bash
# Docker 설치 (Ubuntu)
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER

# Docker Compose 설치
sudo apt-get install docker-compose-plugin

# 재로그인 후 확인
docker --version
docker compose version
```

### Step 2: 프로젝트 업로드

다운로드한 `subsidy-crawler.zip`을 서버에 업로드합니다.

```bash
# 로컬에서 서버로 업로드 (scp 예시)
scp subsidy-crawler.zip user@your-server:/home/user/

# 서버에서 압축 해제
cd /home/user
unzip subsidy-crawler.zip
cd subsidy-crawler
```

### Step 3: 환경 변수 설정

```bash
# .env 파일 생성
cp .env.example .env

# 편집
nano .env
```

아래 항목을 반드시 채우세요:

```ini
# === 필수 ===
BIZINFO_API_KEY=발급받은_API_키

SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SENDER_EMAIL=subsidy-alert@jiransoft.co.kr
SENDER_PASSWORD=앱_비밀번호_16자리

# 수신자 (쉼표로 구분, 공백 없이)
EMAIL_RECIPIENTS=yh.ok@jiransoft.co.kr,mh.ko@jiransoft.co.kr

# === 선택 ===
LLM_MATCHING=false
ANTHROPIC_API_KEY=
```

### Step 4: 빌드 & 실행

```bash
# 이미지 빌드 + 컨테이너 실행
docker compose up -d --build

# 로그 확인
docker compose logs -f crawler
```

### Step 5: 동작 확인

```bash
# 수동으로 전체 파이프라인 1회 실행
docker compose run crawler python main.py run

# 크롤링만 테스트
docker compose run crawler python main.py crawl --site bizinfo

# 이메일 미리보기 생성 (발송 없이)
docker compose run crawler python main.py preview

# 생성된 미리보기 파일 확인
ls data/email_preview_*.html
```

### Step 6: 크론 동작 확인

Docker 컨테이너 내부에 cron이 설정되어 있어 **매주 화/금 오전 10시 (KST)**에 자동 실행됩니다.

```bash
# 컨테이너 내 크론 확인
docker compose exec crawler crontab -l

# 컨테이너 상태 확인
docker compose ps
```

### 주요 Docker 명령어

```bash
docker compose up -d          # 백그라운드 실행
docker compose down            # 중지
docker compose restart         # 재시작
docker compose logs -f         # 실시간 로그
docker compose run crawler python main.py run   # 수동 실행
```

---

## 방법 B: 서버 직접 설치

Docker 없이 Python을 직접 설치하는 방식입니다.

### Step 1: Python 환경 설정

```bash
# Python 3.11+ 설치 (Ubuntu)
sudo apt update
sudo apt install python3.11 python3.11-venv python3-pip -y

# 프로젝트 디렉토리
cd /home/user/subsidy-crawler

# 가상환경 생성
python3.11 -m venv venv
source venv/bin/activate

# 의존성 설치
pip install -r requirements.txt
```

### Step 2: Playwright 설치 (선택 — JS 렌더링 사이트용)

K-Startup, MSSMIV 등 SPA 사이트 크롤링에 필요합니다.

```bash
pip install playwright
playwright install chromium
playwright install-deps  # 시스템 의존성 설치
```

### Step 3: 환경 변수 설정

```bash
cp .env.example .env
nano .env
# (내용은 Docker 배포와 동일)
```

환경 변수를 자동 로드하려면:

```bash
pip install python-dotenv
```

그리고 `main.py` 실행 전에:

```bash
# .env 파일을 자동 로드하는 방법
export $(grep -v '^#' .env | xargs)
```

또는 셸 프로필(`~/.bashrc`)에 추가:

```bash
echo 'set -a; source /home/user/subsidy-crawler/.env; set +a' >> ~/.bashrc
```

### Step 4: DB 초기화 & 테스트

```bash
source venv/bin/activate

# DB 초기화
python main.py init

# 크롤링 테스트
python main.py crawl --site bizinfo

# 매칭 테스트
python main.py match

# 이메일 미리보기
python main.py preview

# 전체 파이프라인 테스트
python main.py run
```

### Step 5: 크론 설정

```bash
# 자동 설정 스크립트 사용
bash setup_cron.sh

# 또는 수동 설정
crontab -e
```

아래 줄을 추가합니다:

```cron
# 정부 지원사업 크롤러: 매주 화/금 오전 10시 (KST) 실행
0 1 * * 2,5 cd /home/user/subsidy-crawler && /home/user/subsidy-crawler/venv/bin/python main.py run >> /home/user/subsidy-crawler/crawler.log 2>&1
```

---

## 배포 후 체크리스트

| 항목 | 확인 방법 |
|------|----------|
| DB 초기화 완료 | `subsidy_crawler.db` 파일 존재 확인 |
| 기업마당 API 연동 | 로그에서 `[bizinfo] API 호출` 확인 (403이면 키 재확인) |
| 크롤링 정상 동작 | `python main.py crawl` → "N건 수집" 로그 확인 |
| 매칭 정상 동작 | `python main.py match` → "N건 매칭 결과" 로그 확인 |
| 이메일 발송 정상 | `python main.py notify` → 수신자 메일함 확인 |
| 크론 설정 완료 | `crontab -l` 에서 스케줄 확인 |
| 로그 파일 | `crawler.log` 파일에 실행 기록 쌓이는지 확인 |


---

## 운영 가이드

### 로그 확인

```bash
# 실시간 로그
tail -f crawler.log

# 최근 크롤링 결과만 보기
grep "크롤링 시작\|완료\|오류" crawler.log | tail -20

# 에러만 보기
grep "ERROR" crawler.log
```

### DB 직접 조회

```bash
sqlite3 subsidy_crawler.db

-- 최근 크롤링된 공고
SELECT title, source_site, deadline FROM announcements
ORDER BY crawled_at DESC LIMIT 10;

-- 매칭 점수 높은 순
SELECT a.title, m.product_name, m.match_score
FROM match_results m
JOIN announcements a ON a.content_hash = m.announcement_hash
ORDER BY m.match_score DESC LIMIT 10;

-- 크롤링 이력
SELECT site_id, status, items_found, new_items, finished_at
FROM crawl_log ORDER BY finished_at DESC LIMIT 10;

.exit
```

### 수신자 변경

`.env` 파일에서 `EMAIL_RECIPIENTS` 값을 수정합니다:

```ini
EMAIL_RECIPIENTS=새수신자@jiransoft.co.kr,기존수신자@jiransoft.co.kr
```

Docker 사용 시 컨테이너 재시작이 필요합니다:

```bash
docker compose restart
```

### 크롤링 사이트 추가

`config/settings.py`의 `CRAWL_SITES` 리스트에 새 사이트를 추가합니다:

```python
{
    "id": "new_site",
    "name": "새로운 사이트",
    "url": "https://www.example.go.kr",
    "list_url": "https://www.example.go.kr/board/list",
    "crawler_type": "html",  # html | api | playwright
    "org": "소관부처",
    "priority": 2,
},
```

테스트 후 배포합니다:

```bash
python main.py crawl --site new_site
```

### 제품 추가 / 키워드 수정

`config/settings.py`의 `PRODUCTS` 딕셔너리에서 제품별 태그를 추가/수정합니다:

```python
"오피스에이전트": {
    "tags": [
        "AI", "RAG", "LLM", ...
        "새로운키워드",  # 추가
    ],
    "weight": 1.5,
},
```

---

## 트러블슈팅

### 크롤링 0건 수집

- **403 Forbidden**: 해당 사이트가 봇 차단. → Playwright 크롤러로 전환하거나 User-Agent 변경
- **SSL 에러**: 인증서 문제. → `verify=False` 설정 이미 적용되어 있음
- **타임아웃**: 서버 응답 지연. → `timeout` 값 증가 (crawler.py)
- **0개 항목 발견**: HTML 구조 변경. → 해당 사이트의 CSS 셀렉터를 `config/settings.py`에서 조정

### 이메일 발송 실패

- **인증 오류**: Gmail 앱 비밀번호 재확인, 2단계 인증 활성화 여부 확인
- **연결 거부**: 서버 방화벽에서 SMTP 포트(587) 아웃바운드 허용 필요
- **수신 안됨**: 스팸 폴더 확인, 발송 이메일 주소의 SPF/DKIM 설정 확인

### 매칭 점수가 너무 낮거나 높음

- `config/settings.py`의 `KEYWORD_SYNONYMS`에 누락된 키워드 추가
- `PRODUCTS`의 `weight` 값 조정
- `MATCH_SCORE_THRESHOLD` 값 조정 (기본: 40점)


---

## 서버 비용 참고

| 서비스 | 사양 | 월 비용 (참고) |
|--------|------|---------------|
| 가비아 VPS | 1 vCPU, 1GB RAM | 약 5,500원/월 |
| AWS EC2 t3.micro | 2 vCPU, 1GB RAM | 약 $8.5/월 |
| 카페24 VPS | 1 vCPU, 1GB RAM | 약 5,500원/월 |
| NCP (네이버클라우드) | Micro | 약 13,000원/월 |

3일에 한 번 실행하는 경량 작업이므로 가장 저렴한 VPS로 충분합니다.

---

## CLI 명령어 요약

```bash
python main.py init       # DB 초기화 (최초 1회)
python main.py run        # 전체 파이프라인 (크롤링→매칭→이메일)
python main.py crawl      # 크롤링만
python main.py crawl --site bizinfo  # 특정 사이트만
python main.py match      # 매칭만
python main.py notify     # 이메일 발송만
python main.py preview    # 이메일 미리보기 (HTML 파일 저장)
```
